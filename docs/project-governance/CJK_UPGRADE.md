# CJK publishing upgrade — execution status

Use [CURRENT_PLAN.md](../../CURRENT_PLAN.md) as the only active execution list. The current UI implementation and its scoped verification are documented in [QUIET_WORKSPACE.md](QUIET_WORKSPACE.md) and [REMOTE_WORKSPACE_VERIFICATION.md](REMOTE_WORKSPACE_VERIFICATION.md).

The quiet workspace, actual-entry CodeMirror integration, baseline strict repairs and web build are now implemented on the approved branch. Remote integration run 34877645243 passed before e226271 was pushed. This is not completion of the entire publishing roadmap.

Shared sanitization, unified parsing, cross-inline typography, resource/stale-render handling, further browser/IME/platform checks, print-engine comparison and SDK packaging remain open. No earlier unpushed-local claim was used as evidence. No deployment, merge or user-platform publication.

The complete prior roadmap/status document is preserved byte-for-byte in [2026-09-14-CJK_UPGRADE.md](archive/2026-09-14-CJK_UPGRADE.md). Its checked/unchecked states describe the earlier checkpoint, not the current one.
